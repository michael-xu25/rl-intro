# OpenRLHF (installed separately in setup_lightning.sh to skip flash-attn dep)
# openrlhf

# Lightning AI SDK (for studio management)
lightning-sdk

# Logging
wandb

# HuggingFace datasets (for GSM8K)
datasets
